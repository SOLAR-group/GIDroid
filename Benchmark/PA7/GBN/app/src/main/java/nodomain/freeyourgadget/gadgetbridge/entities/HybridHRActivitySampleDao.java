package nodomain.freeyourgadget.gadgetbridge.entities;
import android.os.Build;
import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import nodomain.freeyourgadget.gadgetbridge.entities.HybridHRActivitySample;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "HYBRID_HRACTIVITY_SAMPLE".
*/
public class HybridHRActivitySampleDao extends AbstractDao<HybridHRActivitySample, Void> {

    public static final String TABLENAME = "HYBRID_HRACTIVITY_SAMPLE";

    /**
     * Properties of entity HybridHRActivitySample.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Timestamp = new Property(0, int.class, "timestamp", true, "TIMESTAMP");
        public final static Property DeviceId = new Property(1, long.class, "deviceId", true, "DEVICE_ID");
        public final static Property UserId = new Property(2, long.class, "userId", false, "USER_ID");
        public final static Property Steps = new Property(3, int.class, "steps", false, "STEPS");
        public final static Property Calories = new Property(4, int.class, "calories", false, "CALORIES");
        public final static Property Variability = new Property(5, int.class, "variability", false, "VARIABILITY");
        public final static Property Max_variability = new Property(6, int.class, "max_variability", false, "MAX_VARIABILITY");
        public final static Property Heartrate_quality = new Property(7, int.class, "heartrate_quality", false, "HEARTRATE_QUALITY");
        public final static Property Active = new Property(8, boolean.class, "active", false, "ACTIVE");
        public final static Property Wear_type = new Property(9, byte.class, "wear_type", false, "WEAR_TYPE");
        public final static Property HeartRate = new Property(10, int.class, "heartRate", false, "HEART_RATE");
    };

    private DaoSession daoSession;


    public HybridHRActivitySampleDao(DaoConfig config) {
        super(config);
    }
    
    public HybridHRActivitySampleDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"HYBRID_HRACTIVITY_SAMPLE\" (" + //
                "\"TIMESTAMP\" INTEGER  NOT NULL ," + // 0: timestamp
                "\"DEVICE_ID\" INTEGER  NOT NULL ," + // 1: deviceId
                "\"USER_ID\" INTEGER NOT NULL ," + // 2: userId
                "\"STEPS\" INTEGER NOT NULL ," + // 3: steps
                "\"CALORIES\" INTEGER NOT NULL ," + // 4: calories
                "\"VARIABILITY\" INTEGER NOT NULL ," + // 5: variability
                "\"MAX_VARIABILITY\" INTEGER NOT NULL ," + // 6: max_variability
                "\"HEARTRATE_QUALITY\" INTEGER NOT NULL ," + // 7: heartrate_quality
                "\"ACTIVE\" INTEGER NOT NULL ," + // 8: active
                "\"WEAR_TYPE\" INTEGER NOT NULL ," + // 9: wear_type
                "\"HEART_RATE\" INTEGER NOT NULL ," + // 10: heartRate
                "PRIMARY KEY (" +
                "\"TIMESTAMP\" ," +
                "\"DEVICE_ID\" ) ON CONFLICT REPLACE)" + ((Build.VERSION.SDK_INT >= Build.VERSION_CODES.LOLLIPOP) ? " WITHOUT ROWID;" : ";")
        );
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"HYBRID_HRACTIVITY_SAMPLE\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, HybridHRActivitySample entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getTimestamp());
        stmt.bindLong(2, entity.getDeviceId());
        stmt.bindLong(3, entity.getUserId());
        stmt.bindLong(4, entity.getSteps());
        stmt.bindLong(5, entity.getCalories());
        stmt.bindLong(6, entity.getVariability());
        stmt.bindLong(7, entity.getMax_variability());
        stmt.bindLong(8, entity.getHeartrate_quality());
        stmt.bindLong(9, entity.getActive() ? 1L: 0L);
        stmt.bindLong(10, entity.getWear_type());
        stmt.bindLong(11, entity.getHeartRate());
    }

    @Override
    protected void attachEntity(HybridHRActivitySample entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    /** @inheritdoc */
    @Override
    public HybridHRActivitySample readEntity(Cursor cursor, int offset) {
        HybridHRActivitySample entity = new HybridHRActivitySample( //
            cursor.getInt(offset + 0), // timestamp
            cursor.getLong(offset + 1), // deviceId
            cursor.getLong(offset + 2), // userId
            cursor.getInt(offset + 3), // steps
            cursor.getInt(offset + 4), // calories
            cursor.getInt(offset + 5), // variability
            cursor.getInt(offset + 6), // max_variability
            cursor.getInt(offset + 7), // heartrate_quality
            cursor.getShort(offset + 8) != 0, // active
            (byte) cursor.getShort(offset + 9), // wear_type
            cursor.getInt(offset + 10) // heartRate
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, HybridHRActivitySample entity, int offset) {
        entity.setTimestamp(cursor.getInt(offset + 0));
        entity.setDeviceId(cursor.getLong(offset + 1));
        entity.setUserId(cursor.getLong(offset + 2));
        entity.setSteps(cursor.getInt(offset + 3));
        entity.setCalories(cursor.getInt(offset + 4));
        entity.setVariability(cursor.getInt(offset + 5));
        entity.setMax_variability(cursor.getInt(offset + 6));
        entity.setHeartrate_quality(cursor.getInt(offset + 7));
        entity.setActive(cursor.getShort(offset + 8) != 0);
        entity.setWear_type((byte) cursor.getShort(offset + 9));
        entity.setHeartRate(cursor.getInt(offset + 10));
     }
    
    /** @inheritdoc */
    @Override
    protected Void updateKeyAfterInsert(HybridHRActivitySample entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    /** @inheritdoc */
    @Override
    public Void getKey(HybridHRActivitySample entity) {
        return null;
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getDeviceDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getUserDao().getAllColumns());
            builder.append(" FROM HYBRID_HRACTIVITY_SAMPLE T");
            builder.append(" LEFT JOIN DEVICE T0 ON T.\"DEVICE_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN USER T1 ON T.\"USER_ID\"=T1.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected HybridHRActivitySample loadCurrentDeep(Cursor cursor, boolean lock) {
        HybridHRActivitySample entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Device device = loadCurrentOther(daoSession.getDeviceDao(), cursor, offset);
         if(device != null) {
            entity.setDevice(device);
        }
        offset += daoSession.getDeviceDao().getAllColumns().length;

        User user = loadCurrentOther(daoSession.getUserDao(), cursor, offset);
         if(user != null) {
            entity.setUser(user);
        }

        return entity;    
    }

    public HybridHRActivitySample loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<HybridHRActivitySample> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<HybridHRActivitySample> list = new ArrayList<HybridHRActivitySample>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<HybridHRActivitySample> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<HybridHRActivitySample> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
