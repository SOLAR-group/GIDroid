package nodomain.freeyourgadget.gadgetbridge.entities;
import android.os.Build;
import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.SqlUtils;
import de.greenrobot.dao.internal.DaoConfig;

import nodomain.freeyourgadget.gadgetbridge.entities.FitProActivitySample;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "FIT_PRO_ACTIVITY_SAMPLE".
*/
public class FitProActivitySampleDao extends AbstractDao<FitProActivitySample, Void> {

    public static final String TABLENAME = "FIT_PRO_ACTIVITY_SAMPLE";

    /**
     * Properties of entity FitProActivitySample.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Timestamp = new Property(0, int.class, "timestamp", true, "TIMESTAMP");
        public final static Property DeviceId = new Property(1, long.class, "deviceId", true, "DEVICE_ID");
        public final static Property UserId = new Property(2, long.class, "userId", false, "USER_ID");
        public final static Property Steps = new Property(3, int.class, "steps", false, "STEPS");
        public final static Property RawKind = new Property(4, int.class, "rawKind", false, "RAW_KIND");
        public final static Property HeartRate = new Property(5, int.class, "heartRate", false, "HEART_RATE");
        public final static Property CaloriesBurnt = new Property(6, Integer.class, "caloriesBurnt", false, "CALORIES_BURNT");
        public final static Property DistanceMeters = new Property(7, Integer.class, "distanceMeters", false, "DISTANCE_METERS");
        public final static Property Spo2Percent = new Property(8, Integer.class, "spo2Percent", false, "SPO2_PERCENT");
        public final static Property PressureLowMmHg = new Property(9, Integer.class, "pressureLowMmHg", false, "PRESSURE_LOW_MM_HG");
        public final static Property PressureHighMmHg = new Property(10, Integer.class, "pressureHighMmHg", false, "PRESSURE_HIGH_MM_HG");
        public final static Property ActiveTimeMinutes = new Property(11, Integer.class, "activeTimeMinutes", false, "ACTIVE_TIME_MINUTES");
    };

    private DaoSession daoSession;


    public FitProActivitySampleDao(DaoConfig config) {
        super(config);
    }
    
    public FitProActivitySampleDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"FIT_PRO_ACTIVITY_SAMPLE\" (" + //
                "\"TIMESTAMP\" INTEGER  NOT NULL ," + // 0: timestamp
                "\"DEVICE_ID\" INTEGER  NOT NULL ," + // 1: deviceId
                "\"USER_ID\" INTEGER NOT NULL ," + // 2: userId
                "\"STEPS\" INTEGER NOT NULL ," + // 3: steps
                "\"RAW_KIND\" INTEGER NOT NULL ," + // 4: rawKind
                "\"HEART_RATE\" INTEGER NOT NULL ," + // 5: heartRate
                "\"CALORIES_BURNT\" INTEGER," + // 6: caloriesBurnt
                "\"DISTANCE_METERS\" INTEGER," + // 7: distanceMeters
                "\"SPO2_PERCENT\" INTEGER," + // 8: spo2Percent
                "\"PRESSURE_LOW_MM_HG\" INTEGER," + // 9: pressureLowMmHg
                "\"PRESSURE_HIGH_MM_HG\" INTEGER," + // 10: pressureHighMmHg
                "\"ACTIVE_TIME_MINUTES\" INTEGER," + // 11: activeTimeMinutes
                "PRIMARY KEY (" +
                "\"TIMESTAMP\" ," +
                "\"DEVICE_ID\" ) ON CONFLICT REPLACE)" + ((Build.VERSION.SDK_INT >= Build.VERSION_CODES.LOLLIPOP) ? " WITHOUT ROWID;" : ";")
        );
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"FIT_PRO_ACTIVITY_SAMPLE\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, FitProActivitySample entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getTimestamp());
        stmt.bindLong(2, entity.getDeviceId());
        stmt.bindLong(3, entity.getUserId());
        stmt.bindLong(4, entity.getSteps());
        stmt.bindLong(5, entity.getRawKind());
        stmt.bindLong(6, entity.getHeartRate());
 
        Integer caloriesBurnt = entity.getCaloriesBurnt();
        if (caloriesBurnt != null) {
            stmt.bindLong(7, caloriesBurnt);
        }
 
        Integer distanceMeters = entity.getDistanceMeters();
        if (distanceMeters != null) {
            stmt.bindLong(8, distanceMeters);
        }
 
        Integer spo2Percent = entity.getSpo2Percent();
        if (spo2Percent != null) {
            stmt.bindLong(9, spo2Percent);
        }
 
        Integer pressureLowMmHg = entity.getPressureLowMmHg();
        if (pressureLowMmHg != null) {
            stmt.bindLong(10, pressureLowMmHg);
        }
 
        Integer pressureHighMmHg = entity.getPressureHighMmHg();
        if (pressureHighMmHg != null) {
            stmt.bindLong(11, pressureHighMmHg);
        }
 
        Integer activeTimeMinutes = entity.getActiveTimeMinutes();
        if (activeTimeMinutes != null) {
            stmt.bindLong(12, activeTimeMinutes);
        }
    }

    @Override
    protected void attachEntity(FitProActivitySample entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    /** @inheritdoc */
    @Override
    public Void readKey(Cursor cursor, int offset) {
        return null;
    }    

    /** @inheritdoc */
    @Override
    public FitProActivitySample readEntity(Cursor cursor, int offset) {
        FitProActivitySample entity = new FitProActivitySample( //
            cursor.getInt(offset + 0), // timestamp
            cursor.getLong(offset + 1), // deviceId
            cursor.getLong(offset + 2), // userId
            cursor.getInt(offset + 3), // steps
            cursor.getInt(offset + 4), // rawKind
            cursor.getInt(offset + 5), // heartRate
            cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6), // caloriesBurnt
            cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7), // distanceMeters
            cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8), // spo2Percent
            cursor.isNull(offset + 9) ? null : cursor.getInt(offset + 9), // pressureLowMmHg
            cursor.isNull(offset + 10) ? null : cursor.getInt(offset + 10), // pressureHighMmHg
            cursor.isNull(offset + 11) ? null : cursor.getInt(offset + 11) // activeTimeMinutes
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, FitProActivitySample entity, int offset) {
        entity.setTimestamp(cursor.getInt(offset + 0));
        entity.setDeviceId(cursor.getLong(offset + 1));
        entity.setUserId(cursor.getLong(offset + 2));
        entity.setSteps(cursor.getInt(offset + 3));
        entity.setRawKind(cursor.getInt(offset + 4));
        entity.setHeartRate(cursor.getInt(offset + 5));
        entity.setCaloriesBurnt(cursor.isNull(offset + 6) ? null : cursor.getInt(offset + 6));
        entity.setDistanceMeters(cursor.isNull(offset + 7) ? null : cursor.getInt(offset + 7));
        entity.setSpo2Percent(cursor.isNull(offset + 8) ? null : cursor.getInt(offset + 8));
        entity.setPressureLowMmHg(cursor.isNull(offset + 9) ? null : cursor.getInt(offset + 9));
        entity.setPressureHighMmHg(cursor.isNull(offset + 10) ? null : cursor.getInt(offset + 10));
        entity.setActiveTimeMinutes(cursor.isNull(offset + 11) ? null : cursor.getInt(offset + 11));
     }
    
    /** @inheritdoc */
    @Override
    protected Void updateKeyAfterInsert(FitProActivitySample entity, long rowId) {
        // Unsupported or missing PK type
        return null;
    }
    
    /** @inheritdoc */
    @Override
    public Void getKey(FitProActivitySample entity) {
        return null;
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getDeviceDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getUserDao().getAllColumns());
            builder.append(" FROM FIT_PRO_ACTIVITY_SAMPLE T");
            builder.append(" LEFT JOIN DEVICE T0 ON T.\"DEVICE_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN USER T1 ON T.\"USER_ID\"=T1.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected FitProActivitySample loadCurrentDeep(Cursor cursor, boolean lock) {
        FitProActivitySample entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Device device = loadCurrentOther(daoSession.getDeviceDao(), cursor, offset);
         if(device != null) {
            entity.setDevice(device);
        }
        offset += daoSession.getDeviceDao().getAllColumns().length;

        User user = loadCurrentOther(daoSession.getUserDao(), cursor, offset);
         if(user != null) {
            entity.setUser(user);
        }

        return entity;    
    }

    public FitProActivitySample loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<FitProActivitySample> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<FitProActivitySample> list = new ArrayList<FitProActivitySample>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<FitProActivitySample> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<FitProActivitySample> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
